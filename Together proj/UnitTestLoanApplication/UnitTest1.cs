using Microsoft.VisualStudio.TestTools.UnitTesting;
using System;
using BusinessLogic.LoanApplication;
using Database.LoanApplication.DataAccess.Repository;
using Database.LoanApplication.DataAccess.UnitOfWork;

namespace UnitTestLoanApplication
{
    [TestClass]
    public class UnitTest1
    {
        [TestMethod]
        public void GetLoanDetailsByID()
        {
            //// Arrange
            //var controller = new LoanAppBL(IUnitOfWork,);
            //controller.Request = new HttpRequestMessage();
            //controller.Configuration = new HttpConfiguration();

            //// Act
            //var response = controller.Get(10);

            //// Assert
            //Product product;
            //Assert.IsTrue(response.TryGetContentValue<Product>(out product));
            //Assert.AreEqual(10, product.Id);
        }
    }
}
